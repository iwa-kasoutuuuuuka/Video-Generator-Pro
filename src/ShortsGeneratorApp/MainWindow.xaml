<Window x:Class="ShortsGeneratorApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShortsGeneratorApp"
        mc:Ignorable="d"
        Title="Shorts Video Generator PRO v2.6 [Premium Edition]" Height="900" Width="1250"
        Background="#05070A" Foreground="#F8FAFC"
        WindowStartupLocation="CenterScreen"
        Icon="app_icon.png">
    
    <Window.Resources>
        <!-- Modern Colors -->
        <Color x:Key="PrimaryColor">#8B5CF6</Color>
        <Color x:Key="SecondaryColor">#EC4899</Color>
        <Color x:Key="SurfaceColor">#111928</Color>
        <Color x:Key="BorderColor">#374151</Color>
        
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#8B5CF6" Offset="0.0" />
            <GradientStop Color="#EC4899" Offset="1.0" />
        </LinearGradientBrush>

        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background" Value="#111928" />
            <Setter Property="BorderBrush" Value="#374151" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Margin" Value="10" />
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0F172A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#6B7280" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="CaretBrush" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryGradient}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1F2937" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#4B5563" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          Background="{TemplateBinding Background}" 
                                          Foreground="{TemplateBinding Foreground}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="6">
                                            <ContentPresenter />
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="30" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" Margin="10,0,0,0" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                    <Path Grid.Column="1" Fill="#94A3B8" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" />
                                </Grid>
                            </ToggleButton>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#111928" BorderThickness="1" BorderBrush="#374151" CornerRadius="6" />
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#111928" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="gd" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="4" Margin="1">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="gd" Property="Background" Value="#8B5CF6" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="gd" Property="Background" Value="#374151" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Settings & Input -->
        <DockPanel Grid.Column="0">
            <!-- Model Settings -->
            <Border DockPanel.Dock="Top" Style="{StaticResource GlassPanel}">
                <StackPanel Margin="15">
                    <TextBlock Text="Local AI Settings" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryGradient}" Margin="0,0,0,15" />
                    
                    <TextBlock Text="Text Generation (LLM)" Foreground="#94A3B8" FontSize="12" Margin="0,0,0,5" />
                    <ComboBox x:Name="LlmModelCombo" Background="#0F172A" Foreground="White" Margin="0,0,0,15" />

                    <TextBlock Text="Visual Generation (SD)" Foreground="#94A3B8" FontSize="12" Margin="0,0,0,5" />
                    <ComboBox x:Name="SdModelCombo" Background="#0F172A" Foreground="White" Margin="0,0,0,15" />

                    <TextBlock Text="Voice Model (Piper/高音質)" Foreground="#94A3B8" FontSize="12" Margin="0,0,0,5" />
                    <ComboBox x:Name="VoiceModelCombo" Background="#0F172A" Foreground="White" Margin="0,0,0,15" />

                    <CheckBox x:Name="UseGpuCheck" Content="GTX 1650 (CUDA) を優先する" IsChecked="True" Foreground="White" />
                    <TextBlock Text="※VRAM 4GB以下は軽量モデル推奨" Foreground="#64748B" FontSize="10" Margin="20,5,0,10" />

                    <TextBlock Text="Video Config" Foreground="#94A3B8" FontSize="12" Margin="0,15,0,5" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Orientation:" VerticalAlignment="Center" Width="80" />
                        <ComboBox x:Name="OrientationCombo" Width="150">
                            <ComboBoxItem Content="Vertical (9:16)" IsSelected="True" />
                            <ComboBoxItem Content="Horizontal (16:9)" />
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,15">
                        <DockPanel>
                            <TextBlock Text="Target Duration:" />
                            <TextBlock x:Name="DurationValueText" Text="20s" HorizontalAlignment="Right" Foreground="{StaticResource PrimaryGradient}" />
                        </DockPanel>
                        <Slider x:Name="DurationSlider" Minimum="15" Maximum="3600" Value="20" TickFrequency="60" IsSnapToTickEnabled="False" 
                                ValueChanged="DurationSlider_ValueChanged" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,15">
                        <DockPanel>
                            <TextBlock Text="Target Characters (文字数):" />
                            <TextBlock x:Name="CharsValueText" Text="200" HorizontalAlignment="Right" Foreground="{StaticResource PrimaryGradient}" />
                        </DockPanel>
                        <Slider x:Name="TargetCharsSlider" Minimum="50" Maximum="5000" Value="200" TickFrequency="50" IsSnapToTickEnabled="False" 
                                ValueChanged="TargetCharsSlider_ValueChanged" />
                    </StackPanel>

                    <TextBlock Text="Output Destination" Foreground="#94A3B8" FontSize="12" Margin="0,15,0,5" />
                    <Button Content="📁 保存先フォルダを選択" Click="BrowseButton_Click" Height="35" Background="#334155" Foreground="White" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6" />
                            </Style>
                        </Button.Resources>
                    </Button>
                    <TextBlock x:Name="OutputPathTextBlock" Text="未設定 (デスクトップに保存されます)" Foreground="#64748B" FontSize="10" Margin="5,5,0,0" TextWrapping="Wrap" />

                    <TextBlock Text="Background Music (BGM)" Foreground="#94A3B8" FontSize="12" Margin="0,15,0,5" />
                    <DockPanel>
                        <Button Content="🎵" Width="30" Height="35" Background="#334155" Foreground="White" BorderThickness="0" Click="BgmBrowse_Click" Margin="0,0,5,0"/>
                        <ComboBox x:Name="BgmCombo" Background="#0F172A" Foreground="White" Height="35" />
                    </DockPanel>
                </StackPanel>
            </Border>

            <!-- Input Area -->
            <Border Style="{StaticResource GlassPanel}">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="アルゴリズム最適化AI 入力フォーム" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="Gold" />
                    
                    <TextBox Grid.Row="1" x:Name="InputTextBox" Style="{StaticResource ModernTextBox}" 
                             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                             ToolTip="ニュースURL（複数可）または動画にしたい文章を入力してください。"/>
                    <TextBlock Grid.Row="1" Text="ここにURLや本文を入力してください（複数URL可）" IsHitTestVisible="False" Foreground="Gray" Opacity="0.5" VerticalAlignment="Top" Margin="10,10,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=InputTextBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <StackPanel Grid.Row="2" Margin="0,15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Platform" Foreground="#94A3B8" Margin="0,0,0,5" />
                                <StackPanel Orientation="Horizontal">
                                    <RadioButton x:Name="YoutubeRadio" Content="Shorts" IsChecked="True" Foreground="White" Margin="0,0,15,0" />
                                    <RadioButton x:Name="TiktokRadio" Content="TikTok" Foreground="White" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="演出スタイル (Style)" Foreground="#94A3B8" Margin="0,0,0,5" />
                                <ComboBox x:Name="StyleCombo" Background="#0F172A" Foreground="White">
                                    <ComboBoxItem Content="標準 (Standard)" IsSelected="True" />
                                    <ComboBoxItem Content="情熱的 (Passionate)" />
                                    <ComboBoxItem Content="論理的 (Logical/Educational)" />
                                    <ComboBoxItem Content="コメディ (Comedy)" />
                                    <ComboBoxItem Content="ニュース風 (News/Formal)" />
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <Button Grid.Row="3" x:Name="GenerateButton" Content="生成を開始する" Style="{StaticResource ModernButton}" 
                            Click="GenerateButton_Click" Height="45" />
                </Grid>
            </Border>
        </DockPanel>

        <!-- Right Panel: Result & Preview -->
        <Border Grid.Column="1" Style="{StaticResource GlassPanel}">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                    <Border Background="#1E293B" Padding="10,5" CornerRadius="15" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="GpuStatusLight" Width="8" Height="8" Fill="Gray" Margin="0,0,5,0"/>
                            <TextBlock x:Name="GpuStatusText" Text="GPU: OFF" FontSize="12" Foreground="White"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#1E293B" Padding="10,5" CornerRadius="15">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="PiperStatusLight" Width="8" Height="8" Fill="Gray" Margin="0,0,5,0"/>
                            <TextBlock x:Name="PiperStatusText" Text="Piper: OFF" FontSize="12" Foreground="White"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="ResultPanel" Margin="0,10">
                        <!-- Result variations will be here -->
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" Grid.ColumnSpan="2" Background="#CC05070A" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock x:Name="LoadingText" Text="AIモデルをロード中..." FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" />
                <ProgressBar x:Name="LoadingProgressBar" IsIndeterminate="True" Width="300" Height="6" Margin="0,20" Foreground="{StaticResource PrimaryGradient}" />
                <TextBlock Text="※初回は時間がかかる場合があります" Foreground="#94A3B8" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
